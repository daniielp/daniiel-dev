"use client"
import { type NavItem } from "@/types"

import SiteNavbar from "./SiteNavbar"

export function SiteHeader() {
    return (
        <header>
            <SiteNavbar />
        </header>
        
    )
}